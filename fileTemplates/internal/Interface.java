#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};#end
/**
 * ${DESCRIPTION}
 * @author ${USER}
 * @version ${DATE} ${TIME} 
 */
public interface ${NAME} {
    String field;
    
    void method();
}
